export const StockInfo = (props) => {
    return (
        <div className="StockInfo">
            <p>Stock Info POG!</p>
            <div>{props.symbol}</div>
        </div>
    )
}

export default StockInfo